### setup
cargo b -r

### run

sudo ./target/release/xdp-check

### help

sudo ./target/release/xdp-check --help

```
Commands:
  check    default
  kernel   
  nic      
  runtime  verify if XDP is currently active on the system
  quick    
  help     Print this message or the help of the given subcommand(s)
```
